# ray-model-multiplexing
This repository contains the Ray serve application code that uses Ray serve model multiplexing API to efficiently serve pool of models that has same input shapes with difference in weights.

